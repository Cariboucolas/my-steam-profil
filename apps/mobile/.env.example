# Optional. The Steam profile a fresh install starts on: a SteamID64, seventeen
# digits. Leave it empty and the app asks on first launch, which is what a
# deployed build does. Public data, not a secret — find yours at steamid.io
EXPO_PUBLIC_STEAM_ID=

# Where apps/api is listening.
# On a phone, localhost is the phone itself — use the LAN address of the
# machine running the backend, for example http://192.168.1.10:3000
EXPO_PUBLIC_API_URL=http://localhost:3000

# EXPO_PUBLIC_COMMIT_SHA and EXPO_PUBLIC_LIVE are set by the workflows that
# publish, and are deliberately absent here. The app states the commit it was
# built from (ADR-0016); the live flag is what makes it state it plainly
# rather than as `dev`, so a machine that sets it makes its own bundle claim
# to be the live site.
#
# EXPO_PUBLIC_SENTRY_DSN is set there too, and is inert without them: the
# reporter only starts from a build that says it is live (ADR-0017), so a
# development run never files an issue someone has to read.
